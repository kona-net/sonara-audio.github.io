# SONARA Audio — Deployment

Upload the complete project folder to GitHub Pages, Netlify, Vercel static hosting, or another static host.

Keep the relative folder structure intact because the storefront uses local assets and `data/catalog.json`.

For a local preview, run `python -m http.server 8080` from the project root and open `http://localhost:8080/`.
