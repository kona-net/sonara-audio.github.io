# Content Guide

Product photography is local and deterministically assigned per product. Replace assets only by preserving each filename or updating `data/catalog.json`.

Do not place prices, badges, buttons, ratings, or website UI inside product photographs.
